// The picker is shared with the chat composer and lives in `components/ui/`;
// this re-export keeps the feed slice's own imports unchanged.
export { MentionPicker } from "@/components/ui/mention-picker";
